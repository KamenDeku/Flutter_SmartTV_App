name: flutter_smarttv_app 
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter # Incluye el framework Flutter principal
  video_player: ^2.8.6 # video_player -> para reproducir videos locales o por URL
  cupertino_icons: ^1.0.8  # cupertino_icons -> íconos de estilo iOS

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

assets:  # Lista de recursos (assets) que se incluirán en el bundle
    - lib/assets/posters/
    - lib/assets/gameplays/
